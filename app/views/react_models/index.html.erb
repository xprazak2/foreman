<div id="models_page"></div>

<% binding.pry %>
<%= mount_react_component('ModelsPage', '#models_page', {
    searchProps: get_search_props.merge({ :autocomplete => { :useKeyShortcuts => true, :id => 'models-autocomplete-id' }}),
    searchable: true
  }.to_json,
  :flatten_data => true )
%>
