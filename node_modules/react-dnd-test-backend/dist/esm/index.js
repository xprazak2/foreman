import TestBackend from './TestBackend';
var instance;
export function getInstance() {
  return instance;
}
export function clearInstance() {
  instance = undefined;
}

var createBackend = function createBackend(manager) {
  instance = new TestBackend(manager);
  return instance;
};

export default createBackend;